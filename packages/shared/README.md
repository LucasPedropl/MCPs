# @mcps/shared

Tipos e helpers compartilhados entre pacotes do monorepo.

## Exports

- `BridgeProvider`, `JobStatus`, `AgentHost` — tipos do Agent OS
- `jsonText`, `errorText`, `markdownText` — helpers de resposta MCP

```ts
import { JobStatus, jsonText } from "@mcps/shared";
```
